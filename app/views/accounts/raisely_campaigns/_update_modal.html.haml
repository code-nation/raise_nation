.modal-dialog{:role => "document"}
  .modal-content
    .modal-header
      %h5.modal-title Update Campaign
      %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
        %span{"aria-hidden" => "true"} ×
    .modal-body
      = bootstrap_form_for @campaign, url: accounts_raisely_campaign_path(account_id: @account.id, id: @campaign.id) do |f|
        = f.text_field :api_key, value: ''
        = f.submit 'Update Campaign', class: 'btn btn-primary btn-sm update-campaign-submit'
